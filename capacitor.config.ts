import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig={appId:'de.schulplaner.app',appName:'Schulplaner',webDir:'dist',ios:{contentInset:'automatic',preferredContentMode:'mobile'}}
export default config
